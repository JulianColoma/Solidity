// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

/*
 * 🎓 EJERCICIO 1: Bucle de Cuenta Regresiva
 *
 * Construye una sumatoria consecutiva progresiva. 
 * Dada una cuota numérica variable ingresada de tipo `n`, desarrolla una iteración estricta 
 * que incremente linealmente de uno en uno hasta llegar y alcanzar exactamente a dicho límite numérico `n`.
 * Durante el proceso, acumula matemáticamente todo el peso interior de sus ciclos, para finalmente 
 * expulsar orgánicamente el resultado totalizado de la suma en una devolución final al usuario. 
 *
 *
 * 🎓 EJERCICIO 2: Iterar hasta una condición (Buscador Seguro)
 * 
 * Crea un buscador de tesoro numérico preventivo. Imagina, declara y pre-rellena 
 * un repositorio de cifras en formato de lista en el contrato.
 * 
 * Un usuario introducirá una clave numérica con la intención de encontrar si se halla allí dentro. 
 * Construye un ciclo sistemático y secuencial; sin embargo, no despilfarres recursos: 
 * si la lógica halla el elemento verídico buscado antes de llegar al tope natural predeterminado del listado, 
 * deberás abortar irremediablemente el ciclo actual para aislarte, cortando en seco y de repente la iteración continuada 
 * hacia el resto del arreglo, retornando afirmativo e impidiendo el gasto excesivo y mortal del gas bloqueante.
 */

contract EjerciciosLoops {
    // 📝 Escribe tu código aquí debajo

}
