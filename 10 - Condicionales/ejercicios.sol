// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

/*
 * 🎓 EJERCICIO 1: Verificación de Elegibilidad
 *
 * Implementa un sistema de control de acceso por edad. Tu contrato debe evaluar matemáticamente 
 * el nivel de madurez ingresado por quien llama. 
 * Si dicho nivel supera o iguala la mayoría de edad (18), provéele de una autorización afirmativa en texto ("Autorizado"). 
 * De lo contrario, deberás interrumpirlo delegando una desautorización precisa ("Denegado"). 
 * Utiliza la estructura fundamental de decisión en bloque lógica clásica de programación.
 *
 *
 * 🎓 EJERCICIO 2: Optimización del código 
 * 
 * El ejercicio anterior funciona, pero consume demasiadas líneas de código para algo binario simple. 
 * Replantea tu función anterior (llamándola de manera distinta) logrando exactamente la misma asignación 
 * y evaluación o retorno, pero resolviendo toda la toma de decisión evaluativa en **una sola línea** 
 * optimizando los recursos y la estética visual de la sintaxis abreviada provista por el lenguaje.
 */

contract EjerciciosCondicionales {
    // 📝 Escribe tu código aquí debajo

}
